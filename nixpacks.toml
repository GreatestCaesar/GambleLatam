[phases.setup]
nixPkgs = [
  "python311",
  "pip"
]

[phases.install]
cmds = [
  "apt-get update -qq",
  "apt-get install -y -qq libglib2.0-0 libnss3 libnspr4 libdbus-1-3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxkbcommon0 libatspi2.0-0 libx11-6 libxcomposite1 libxdamage1 libxext6 libxfixes3 libxcb1 libcairo2 libxrandr2 libgbm1 libasound2 libpango-1.0-0 || true",
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[phases.build]
cmds = []

[start]
cmd = "python webhook.py"

